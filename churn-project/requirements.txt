pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
xgboost>=2.0        # optional — project falls back to GradientBoosting
shap>=0.44          # optional — for SHAP explainability plots
imbalanced-learn>=0.11  # optional — for SMOTE oversampling
